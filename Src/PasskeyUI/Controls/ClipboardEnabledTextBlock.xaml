<UserControl x:Class="DSInternals.Win32.WebAuthn.PasskeyUI.ClipboardEnabledTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="200"
             x:Name="Root"
             Background="Transparent"
             Focusable="False">
    <Grid Background="Transparent">
        <TextBlock Text="{Binding Text, ElementName=Root}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
        <Button ToolTip="Copy to clipboard"
                Style="{StaticResource OverlayCopyButtonStyle}"
                Click="CopyButton_Click">
            <TextBlock Text="&#xE8C8;" FontFamily="{StaticResource FluentIcons}" FontSize="14" />
        </Button>
    </Grid>
</UserControl>
