<UserControl x:Class="DSInternals.Win32.WebAuthn.PasskeyUI.KeyGenerationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <Label Content="Algorithm:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
        <ComboBox ItemsSource="{Binding Algorithms}"
                  SelectedValue="{Binding SelectedAlgorithm, Mode=TwoWay}"
                  DisplayMemberPath="Value"
                  SelectedValuePath="Key"
                  Grid.Row="0" Grid.Column="1"
                  VerticalAlignment="Center" />

        <Button Command="{Binding SaveCommand}"
                Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                HorizontalAlignment="Center"
                Margin="0,15,0,5"
                IsDefault="True">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#xE74E;" FontFamily="{StaticResource FluentIcons}" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                <AccessText Text="_Save" VerticalAlignment="Center" />
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
