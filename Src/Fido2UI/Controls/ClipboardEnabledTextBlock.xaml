<UserControl x:Class="DSInternals.Win32.WebAuthn.Fido2UI.ClipboardEnabledTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="200"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="OverlayCopyButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
            <Setter Property="Visibility" Value="Hidden" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="4,2" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=DataGridCell}}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <TextBlock Text="{Binding Text, ElementName=Root}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
        <Button Content="📋" ToolTip="Copy to clipboard"
                Style="{StaticResource OverlayCopyButtonStyle}"
                Click="CopyButton_Click" />
    </Grid>
</UserControl>
